[package]
name = "greeter_lib"
version = "0.1.0"
edition = "2024"

[dependencies]
lirpc_rs_client = { git = "ssh://git@codeberg.org/donkere-vader/lirpc" }
serde = { version = "1.0.228", features = ["derive"] }
