# Example contracts

These are the contract files that were autogenerated from the examples in of the `lirpc` crate.

## Compilation of these contracts

```shell
cargo run --bin lirpc-cli -- compile lirpc EXAMPLE_NAME example-contracts/EXAMPLE_NAME.json 0.1.0
```
